{
  "pluginAlias": "WindowsSpeaker",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "homebridge-windows-speaker"
      },
      "refreshButton": {
        "title": "Show refresh button",
        "type": "boolean",
        "required": false,
        "default": true
      },
      "autoRefresh": {
        "title": "Auto refresh",
        "type": "boolean",
        "required": false,
        "default": false
      },
      "autoRefreshInterval": {
        "title": "Auto refresh interval (seconds)",
        "type": "number",
        "required": false,
        "default": 10
      }
    }
  }
}